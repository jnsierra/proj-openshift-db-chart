apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "proj-openshift-chart.fullname" . }}-test-connection"
  labels:
    {{- include "proj-openshift-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "proj-openshift-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
